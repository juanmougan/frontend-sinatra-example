{
  "name": "frontend-sinatra-example",
  "engines": {
    "node": "13.5.0",
    "yarn": "1.3.2"
  },
  "scripts": {
    "build": "yarn --cwd ui install && yarn --cwd ui build",
    "deploy": "cp -a ui/build/. public/",
    "heroku-postbuild": "yarn build && yan deploy"
  },
  "author": "Juan Manuel Mougan"
}
