{
  "name": "frontend-sinatra-example",
  "engines": {
    "node": "13.5.0",
    "yarn": "1.3.2"
  },
  "scripts": {
    "install": "yarn --cwd ui install",
    "deploy": "cp -a ui/public/. public/",
    "heroku-postbuild": "yarn install && yarn deploy"
  },
  "author": "Juan Manuel Mougan"
}
